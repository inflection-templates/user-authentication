/**
 * Dependency injection container for Order Service
 * Equivalent to the user-service injector
 */

import { container } from 'tsyringe';

export class Injector {
    static Container = container;
}
