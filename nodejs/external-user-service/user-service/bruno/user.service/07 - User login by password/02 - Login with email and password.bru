meta {
  name: 02 - Login with email and password
  type: http
  seq: 2
}

post {
  url: {{BASE_URL}}/auth/login-with-email-password
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  x-api-key: {{API_CLIENT_INTERNAL_KEY}}
}

body:json {
  {
    "Email": "sys.admin@inflectionzone.com",
    "Password": "uHqLYqjh"
  }
}

tests {
  try {
      var jsonRes = res.getBody();
      bru.setEnvVar("USER_JWT", jsonRes.Data.AccessToken);
  }
  catch (error) {
      console.log(error.message);
  }
  
}
